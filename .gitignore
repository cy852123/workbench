# ===== 个人工作台 · 哪些东西不入库 =====
# 原则：仓库只放「源码 + 图标」，产物 / 凭据 / 个人数据 / 截图 全部留在磁盘上不入库。
# 这些文件不会因为被 ignore 而消失，只是不再被 git 跟踪。

## 依赖
node_modules/
__pycache__/
*.pyc

## 凭据（绝不上传；本地留着，同步与部署要用）
.cf-env
.sync-key.txt

## 个人数据（云端同步快照、导出的备份；含同步密钥，千万别入库）
downloads/
data_tmp.json
wb_cloud_check.json
wb_now.json

## 测试与探针
# tests/ 入库了 —— 生产门禁必须跟着代码走（回归网没有版本控制等于没有回归网）。
# 现在 tests/ 只放三个文件：test.js（桌面+手机三尺寸）、test_interact.js（交互与防回归）、
# baseline.json（单一基线：所有随数据/界面变化的数字只在这里维护一处）。
# 一次性探针、历史版本测试、旧截图：已归档到 _attic/tests-archive/（_attic/ 整目录不入库）。
tests/*.png
logs/
*.bak
_dump.py
_inspect.py
*.tmp
*.log

## 归档（历史垃圾都挪进这里，不删）
_attic/

## 构建产物 / 部署暂存 / 截图（文件留在磁盘，部署脚本直接用）
.pages-deploy/
.wrangler/
previews/
shots/
shot_*.png

## 词典源数据（66MB CSV + 5MB zip，用 download_dict.py 重新下载；dict.js 由 gen_dict.py 生成）
ecdict_full.csv
ecdict.zip
dict.js
